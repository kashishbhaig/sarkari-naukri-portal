import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// UPDATED METADATA WITH GOOGLE VERIFICATION
export const metadata: Metadata = {
  title: "SNP - Sarkari Naukri Prayas | Online Test Portal",
  description: "Best platform for Indian Government Exam preparation, Mock Tests and Quizzes.",
  verification: {
    google: "YfXZINorov90Fb9gnGS1i2HtC73qgl40S_BBhLpl_VE",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="hi"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-slate-50 text-slate-900">
        {children}
      </body>
    </html>
  );
}
